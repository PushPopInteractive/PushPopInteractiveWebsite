# Returnity screenshots — awaiting art

Drop gameplay captures here (PNG/JPG, any names), then run:

    python3 tools/ingest_shots.py     # normalizes to ../shot-N.jpg
    python3 tools/build_site.py       # card strip + game page pick them up

The card shows the first three; the game page shows all of them.
